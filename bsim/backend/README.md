# bsim backend

## Getting started

Install dependencies (one-time):

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Start the backend:

```bash
source .venv/bin/activate
python main.py
```

